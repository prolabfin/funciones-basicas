//Crea una funcion que recorra desde el numero cero(0) al 9, y que con cada iteración se le aplique una suma de 2 unidades. 

//============
function contar() {
  for(var c=0; c<10; c++) {
    c = c + 2;
    console.log(c);
  }
}
contar();